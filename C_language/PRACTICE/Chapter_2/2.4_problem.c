#include <stdio.h>

int main(){
    int x = 2, y = 3, z = 3, k = 1;
    float ans = 3*x/y - z + k; // int bhi use kar skte h.
    printf("The value of ans is %f\n", ans);

    // 3*x/y - z + k
    // 3*2/3 - 3 + 1
    // 6/3 - 4
    // 2 - 2
    // 0

    return 0;
}
