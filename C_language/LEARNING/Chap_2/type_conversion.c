#include <stdio.h>

int main(){
    int a = 9;
    // int b = 2;
    // float c = a/b; //the output is 4.0000 because the inputs are integers.
    float b = 2.0;
    float c = a/b;
    
    int d = 6.7;
    printf("The vslue of a/b is %f\n", c); 
    printf("the value of d is %d\n", d);

    return 0;

    
}
