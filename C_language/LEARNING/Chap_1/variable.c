#include <stdio.h>

int main()
{
    int a = 5;  // declaration + initialization
    int b;      // declaration

    printf("Enter the value of b: ");
    scanf("%d", &b);

    printf("a = %d\n", a);
    printf("b = %d\n", b);

    return 0;
}